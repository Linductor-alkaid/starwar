# 服务器部署指南

本文档将指导您如何将星际防卫战项目部署到服务器上。

## 📋 部署前准备

### 1. 服务器环境要求

- **操作系统**: Linux (推荐 Ubuntu 20.04+ 或 CentOS 7+)
- **JDK**: JDK 8 或更高版本
- **MySQL**: MySQL 5.7+ 或 MariaDB 10.3+
- **Nginx**: Nginx 1.18+ (用于部署前端静态文件)
- **Maven**: Maven 3.6+ (可选，用于在服务器上构建)

### 2. 本地准备的文件

在开始部署前，确保您已经准备好以下文件：

- ✅ 前端构建产物：`frontend/dist/` 目录（已完成 `npm run build`）
- ✅ 后端JAR包：`backend/target/star-war-backend-1.0.0.jar`（或使用Maven在服务器上打包）

---

## 🚀 部署步骤

### 步骤1：上传文件到服务器

#### 方式1：使用SCP命令（Linux/Mac）

```bash
# 上传前端构建产物
scp -r frontend/dist/* root@your-server-ip:/var/www/starwar/

# 上传后端JAR包
scp backend/target/star-war-backend-1.0.0.jar root@your-server-ip:/opt/starwar/
```

#### 方式2：使用WinSCP或FileZilla（Windows）

1. 使用FTP工具连接到服务器
2. 将 `frontend/dist/` 目录下的所有文件上传到 `/var/www/starwar/`
3. 将 `backend/target/star-war-backend-1.0.0.jar` 上传到 `/opt/starwar/`

#### 方式3：使用压缩包方式

```bash
# 在本地压缩
# Windows PowerShell:
Compress-Archive -Path frontend\dist\* -DestinationPath frontend-dist.zip
Compress-Archive -Path backend\target\star-war-backend-1.0.0.jar -DestinationPath backend.jar.zip

# 上传后解压
# Linux服务器上:
unzip frontend-dist.zip -d /var/www/starwar/
unzip backend.jar.zip -d /opt/starwar/
```

---

### 步骤2：配置数据库

#### 2.1 在服务器上安装MySQL（如果未安装）

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install mysql-server -y
sudo systemctl start mysql
sudo systemctl enable mysql
```

**CentOS/RHEL:**
```bash
sudo yum install mysql-server -y
sudo systemctl start mysqld
sudo systemctl enable mysqld
```

#### 2.2 创建数据库和用户

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE star_war_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 创建用户（可选，建议使用独立用户）
CREATE USER 'starwar'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON star_war_db.* TO 'starwar'@'localhost';
FLUSH PRIVILEGES;

# 导入数据库结构（从您的init.sql文件）
# 将 docs/database/init.sql 上传到服务器后执行：
source /path/to/init.sql

# 或者直接执行SQL语句
exit
mysql -u root -p star_war_db < /path/to/init.sql
```

---

### 步骤3：部署后端服务

#### 3.1 安装JDK 8

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install openjdk-8-jdk -y
java -version  # 验证安装
```

**CentOS/RHEL:**
```bash
sudo yum install java-1.8.0-openjdk java-1.8.0-openjdk-devel -y
java -version  # 验证安装
```

#### 3.2 创建应用目录

```bash
sudo mkdir -p /opt/starwar
sudo mkdir -p /opt/starwar/logs
sudo mkdir -p /opt/starwar/config

# 设置权限
sudo chown -R $USER:$USER /opt/starwar
```

#### 3.3 配置application.yml

在服务器上创建配置文件：

```bash
sudo mkdir -p /opt/starwar/config
sudo nano /opt/starwar/config/application.yml
```

配置文件内容（**根据您的服务器实际情况修改**）：

```yaml
server:
  port: 8080

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/star_war_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root  # 或使用您创建的用户名
    password: your_mysql_password  # 修改为实际密码

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.starwar.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.starwar: INFO
    org.springframework.web: INFO
  file:
    name: /opt/starwar/logs/starwar.log
```

**重要**: 
- 修改数据库密码为实际密码
- 如果MySQL不在localhost，修改数据库连接地址
- 如果使用外部数据库，确保服务器可以访问数据库端口（3306）

#### 3.4 创建启动脚本

创建启动脚本 `start.sh`:

```bash
sudo nano /opt/starwar/start.sh
```

脚本内容：

```bash
#!/bin/bash

APP_NAME="star-war-backend"
APP_JAR="star-war-backend-1.0.0.jar"
APP_DIR="/opt/starwar"
PID_FILE="$APP_DIR/$APP_NAME.pid"

cd $APP_DIR

# 检查是否已经运行
if [ -f "$PID_FILE" ]; then
    PID=$(cat $PID_FILE)
    if ps -p $PID > /dev/null 2>&1; then
        echo "$APP_NAME is already running with PID $PID"
        exit 1
    else
        rm -f $PID_FILE
    fi
fi

# 启动应用
nohup java -jar \
    -Dspring.config.location=file:$APP_DIR/config/application.yml \
    -Xms512m \
    -Xmx1024m \
    $APP_JAR > logs/startup.log 2>&1 &

echo $! > $PID_FILE
echo "$APP_NAME started with PID $(cat $PID_FILE)"
```

创建停止脚本 `stop.sh`:

```bash
sudo nano /opt/starwar/stop.sh
```

脚本内容：

```bash
#!/bin/bash

APP_NAME="star-war-backend"
APP_DIR="/opt/starwar"
PID_FILE="$APP_DIR/$APP_NAME.pid"

if [ -f "$PID_FILE" ]; then
    PID=$(cat $PID_FILE)
    if ps -p $PID > /dev/null 2>&1; then
        kill $PID
        echo "$APP_NAME stopped (PID: $PID)"
        rm -f $PID_FILE
    else
        echo "$APP_NAME is not running"
        rm -f $PID_FILE
    fi
else
    echo "$APP_NAME is not running"
fi
```

设置执行权限：

```bash
chmod +x /opt/starwar/start.sh
chmod +x /opt/starwar/stop.sh
```

#### 3.5 创建systemd服务（推荐，实现开机自启）

```bash
sudo nano /etc/systemd/system/starwar.service
```

服务文件内容：

```ini
[Unit]
Description=Star War Backend Application
After=network.target mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/starwar
ExecStart=/usr/bin/java -jar -Dspring.config.location=file:/opt/starwar/config/application.yml -Xms512m -Xmx1024m /opt/starwar/star-war-backend-1.0.0.jar
Restart=always
RestartSec=10
StandardOutput=append:/opt/starwar/logs/starwar.log
StandardError=append:/opt/starwar/logs/starwar-error.log

[Install]
WantedBy=multi-user.target
```

启用并启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable starwar.service
sudo systemctl start starwar.service
sudo systemctl status starwar.service
```

查看日志：

```bash
# 实时查看日志
sudo journalctl -u starwar.service -f

# 或查看应用日志
tail -f /opt/starwar/logs/starwar.log
```

---

### 步骤4：部署前端（使用Nginx）

#### 4.1 安装Nginx

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install nginx -y
sudo systemctl start nginx
sudo systemctl enable nginx
```

**CentOS/RHEL:**
```bash
sudo yum install nginx -y
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### 4.2 配置Nginx

创建Nginx配置文件：

```bash
sudo nano /etc/nginx/sites-available/starwar
```

**注意**: CentOS/RHEL 使用 `/etc/nginx/conf.d/starwar.conf`

配置文件内容（**根据实际情况修改域名和端口**）：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 修改为您的域名或服务器IP

    # 前端静态文件目录
    root /var/www/starwar;
    index index.html;

    # 前端路由（Vue Router history模式）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 后端API代理
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 日志
    access_log /var/log/nginx/starwar_access.log;
    error_log /var/log/nginx/starwar_error.log;
}
```

#### 4.3 启用配置

**Ubuntu/Debian:**
```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/starwar /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重载Nginx
sudo systemctl reload nginx
```

**CentOS/RHEL:**
```bash
# 配置文件已在正确位置，直接测试
sudo nginx -t

# 重载Nginx
sudo systemctl reload nginx
```

#### 4.4 设置目录权限

```bash
sudo chown -R www-data:www-data /var/www/starwar  # Ubuntu/Debian
# 或
sudo chown -R nginx:nginx /var/www/starwar  # CentOS/RHEL

sudo chmod -R 755 /var/www/starwar
```

---

### 步骤5：配置防火墙

#### Ubuntu (UFW)

```bash
# 允许HTTP和HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 如果需要SSH（如果未启用）
sudo ufw allow 22/tcp

# 启用防火墙
sudo ufw enable

# 查看状态
sudo ufw status
```

#### CentOS (firewalld)

```bash
# 允许HTTP和HTTPS
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https

# 重载防火墙
sudo firewall-cmd --reload

# 查看状态
sudo firewall-cmd --list-all
```

---

## ✅ 验证部署

### 1. 检查后端服务

```bash
# 检查服务状态
sudo systemctl status starwar.service

# 检查端口是否监听
sudo netstat -tlnp | grep 8080
# 或
sudo ss -tlnp | grep 8080

# 测试API接口
curl http://localhost:8080/api/v1/game/leaderboard?page=1&size=10
```

### 2. 检查Nginx

```bash
# 检查Nginx状态
sudo systemctl status nginx

# 检查Nginx配置
sudo nginx -t

# 测试前端访问
curl http://localhost
```

### 3. 浏览器访问

在浏览器中访问：
- **前端地址**: `http://your-server-ip` 或 `http://your-domain.com`
- **Swagger文档**: `http://your-server-ip/api/swagger-ui.html` (如果后端启用了Swagger)

---

## 🔧 常用操作命令

### 后端服务管理

```bash
# 启动服务
sudo systemctl start starwar.service

# 停止服务
sudo systemctl stop starwar.service

# 重启服务
sudo systemctl restart starwar.service

# 查看状态
sudo systemctl status starwar.service

# 查看日志
sudo journalctl -u starwar.service -f
tail -f /opt/starwar/logs/starwar.log
```

### Nginx管理

```bash
# 启动
sudo systemctl start nginx

# 停止
sudo systemctl stop nginx

# 重启
sudo systemctl restart nginx

# 重载配置（不中断服务）
sudo systemctl reload nginx

# 测试配置
sudo nginx -t

# 查看日志
sudo tail -f /var/log/nginx/starwar_access.log
sudo tail -f /var/log/nginx/starwar_error.log
```

---

## 🔒 安全建议

### 1. 使用HTTPS（推荐）

安装Certbot申请免费SSL证书：

```bash
# Ubuntu/Debian
sudo apt install certbot python3-certbot-nginx -y

# CentOS/RHEL
sudo yum install certbot python3-certbot-nginx -y

# 申请证书
sudo certbot --nginx -d your-domain.com

# 自动续期测试
sudo certbot renew --dry-run
```

### 2. 数据库安全

- 使用强密码
- 限制数据库用户权限
- 不要将数据库暴露到公网
- 定期备份数据库

### 3. 服务器安全

- 定期更新系统包
- 使用SSH密钥认证，禁用密码登录
- 配置fail2ban防止暴力破解
- 仅开放必要的端口

### 4. 应用安全

- 不要在配置文件中硬编码敏感信息
- 使用环境变量管理配置
- 定期更新依赖包
- 监控应用日志

---

## 🐛 常见问题排查

### 问题1: 后端服务无法启动

**检查步骤:**
1. 查看日志：`tail -f /opt/starwar/logs/starwar.log`
2. 检查JDK是否正确安装：`java -version`
3. 检查端口是否被占用：`sudo netstat -tlnp | grep 8080`
4. 检查数据库连接是否正常
5. 检查配置文件路径和内容是否正确

### 问题2: 前端无法访问后端API

**检查步骤:**
1. 检查后端服务是否运行：`sudo systemctl status starwar.service`
2. 检查Nginx配置中的proxy_pass是否正确
3. 检查防火墙是否阻止了端口
4. 查看Nginx错误日志：`tail -f /var/log/nginx/starwar_error.log`
5. 测试后端接口：`curl http://localhost:8080/api/v1/game/leaderboard`

### 问题3: 前端页面404错误

**检查步骤:**
1. 检查Nginx的root目录是否正确：`ls -la /var/www/starwar`
2. 检查index.html是否存在
3. 检查Nginx配置中的try_files是否正确
4. 检查文件权限：`ls -la /var/www/starwar`

### 问题4: 数据库连接失败

**检查步骤:**
1. 检查MySQL服务是否运行：`sudo systemctl status mysql`
2. 检查数据库用户和密码是否正确
3. 检查数据库是否存在：`mysql -u root -p -e "SHOW DATABASES;"`
4. 检查MySQL是否允许localhost连接
5. 查看应用日志中的数据库连接错误信息

---

## 📝 更新部署

当您需要更新代码时：

### 更新后端

```bash
# 1. 停止服务
sudo systemctl stop starwar.service

# 2. 备份当前版本
cp /opt/starwar/star-war-backend-1.0.0.jar /opt/starwar/backup/star-war-backend-1.0.0.jar.$(date +%Y%m%d_%H%M%S)

# 3. 上传新版本JAR包到 /opt/starwar/

# 4. 启动服务
sudo systemctl start starwar.service

# 5. 检查状态
sudo systemctl status starwar.service
```

### 更新前端

```bash
# 1. 备份当前版本
sudo cp -r /var/www/starwar /var/www/starwar.backup.$(date +%Y%m%d_%H%M%S)

# 2. 上传新的dist目录内容到 /var/www/starwar/

# 3. 重载Nginx（通常不需要重启）
sudo systemctl reload nginx
```

---

## 📚 相关文档

- 后端开发文档：`backend/README.md`
- 终端启动指南：`backend/终端启动指南.md`
- 数据库初始化脚本：`docs/database/init.sql`

---

## 🎉 部署完成

恭喜！如果一切顺利，您现在可以通过浏览器访问您的应用了！

**访问地址**: `http://your-server-ip` 或 `http://your-domain.com`

如有任何问题，请查看日志文件或联系技术支持。

